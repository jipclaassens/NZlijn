container GTFS : using = "geometries"
{
	#include<LoadFeeds.dms>
	#include<StudyAreaFeeds.dms>

	// container StaticNets          := templates/StaticNetsCreator(StudyAreaFeeds/Stops, StudyAreaFeeds/Links);
}